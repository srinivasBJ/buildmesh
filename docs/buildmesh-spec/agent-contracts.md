# Agent contracts

Agents read project-scoped evidence and graph state, write auditable agent-run records, and create only grounded recommendations. They cannot alter tasks directly. Recommendation creation requires unique supporting evidence; task materialization occurs only in the atomic reviewer approval transaction.
