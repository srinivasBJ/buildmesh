# BuildMesh product thesis

BuildMesh is a persistent construction intelligence layer. It turns scoped project state, evidence, observations, schedules, dependencies, materials, people, agent decisions, and approvals into an auditable project graph.

The core loop is: **observe → understand → verify → reason → recommend → human review → act → remember**. Agents are bounded: they produce evidence-backed recommendations, not autonomous engineering approvals.
