# BuildMesh scenarios

`buildmesh evaluate --scenario all --json` executes deterministic structured workflow checks. Scenario outcomes intentionally assess state, evidence citations, approval gates, and idempotence—not exact recommendation prose. SCENARIO-007 is currently `PARTIAL`: stale environmental context is a registered requirement but has no production freshness engine.
